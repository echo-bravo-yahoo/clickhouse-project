export const defaultDataPath = "./config/sampleData.json";
export const backend = {
  protocol: "http",
  url: "localhost",
  port: "3001",
};
export const frontend = {
  protocol: "http",
  url: "localhost",
  port: "3000",
};
export const databaseFilePath = "./tmp/runtime/ext/db.json";
